1.0-alpha1
-----------
增加了服务器互连的功能，同时去掉了服务器集群的功能

增加了群组的功能，包括聊天室和游戏大厅

增加了一个网络五子棋游戏

改正了在有些网络环境下，每5分钟左右自动掉线的问题


0.9-beta2
-----------
改正了在系统消息对话框中，点击“添加好友”或“接受请求"按钮后对话框自动关闭的问题

改正了在OutBarCtrl中输入好友的第一个字母自动滚动到相应好友中存在的问题

改正了按快捷键后，系统消息对话框不在最前的问题

改正了有些情况下HTTP代理不能连通的问题

改正了退出程序后，陌生人没有被删除的问题

放宽了对程序掉线的判断条件

将HTTP代理返回的信息记录在c:\myicqhttp.log中


0.9-beta1
-----------
改正了在OutBar中有时候好友会跳到其他组的问题

改正了自定义快捷键(Hot Key)的问题

改正了消息编辑窗口对话模式时自动滚动的问题

改正了消息编辑窗口中有时候双击聊天记录无效的问题

改正了弹出收发消息窗口时有时“最小化”(不在最前)的问题

改正了任务条风格时，主窗口隐藏/显示时其它窗口会失去焦点的问题


0.9-alpha2
-----------
改正了收发文件或者二人世界退出时，有时侯会非法操作的问题

改正了发送消息时有时会出现乱码的问题

改正了发送超长消息时会非法操作的问题

改正了通过HTTP代理连不到服务器的问题

在主窗口中选择组的时候，加了动画效果


0.9-alpha1
-----------
改正了内存泄漏的问题(由于ExtractIcon之后，没有DestroyIcon的原因:-P)

改正了向在线的通过http代理登陆的用户发消息，自己就掉线的问题

改正了处于同一NAT网络，好友之间不能直接通讯的问题

改正了自动状态切换只限于MyICQ主窗口的问题(用hook实现)

改正了最小化主窗口时，收发消息窗口也随之最小化的问题

点对点发送消息时，从UDP改成了TCP协议，使通讯更加安全，也使代码更清晰

发送消息时，增加了表情符号和选择字体(Cool!)

URL的自动识别(RichEdit20a)

群发消息时也可以发送给离线的好友


0.8-alpha1
----------
改正了在主窗口得到焦点时，按回车就退出整个程序的问题

点对点发消息时，从TCP改成了UDP，具有更好的穿透防火墙的能力

采用强大的Berkeley DB存储好友信息和聊天记录，放弃了以前自己写的基于链表式的存储结构

自己重新写了CGfxOutBarCtrl，改正了很多以前的错误，同时也使代码更清晰、更易于维护

支持代理服务器(SOCKS5/HTTP)

向在线的一组好友发送消息

皮肤系统

消息管理(可导入/导出聊天记录)

基于插件，使扩展更容易。提供的插件有:

  收发文件
  二人世界(可以实时语音聊天)
  闹钟提醒


0.1
----

收发(离线)消息(如果客户端之间能直接通讯，则通过TCP协议发送，否则通过服务器中转)

收发文件

添加/删除好友(可以设置身份验证)

服务器端存储好友列表

在客户端存储好友资料和聊天记录
